{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "curl -fsSL https://bun.sh/install | bash && ~/.bun/bin/bun install --no-frozen-lockfile",
  "buildCommand": "~/.bun/bin/bun run build"
}